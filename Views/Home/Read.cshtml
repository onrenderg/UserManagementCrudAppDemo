@model List<UserManagementCrudAppDemo.Models.User>
// “Bind this view to a list of User objects so we can loop over them.”
<!DOCTYPE html>
<!-- “Use HTML5 doctype for standards-compliant rendering.” -->

<html>
<!-- “Start of the HTML document.” -->

<head>
    <!-- “Begin the head section for CSS and style definitions.” -->

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous">
    <!-- “Import Bootstrap CSS for layout and styling.” -->

    <style>
        .custom-font {
            font-family: 'Roboto Slab', serif;
        }

        table {
            width: 100%;
        }

            table input[type="text"],
            table input[type="email"] {
                width: 100%;
            }
    </style>
    <!-- “Define styles for the font and ensure table inputs stretch full width.” -->

</head>
<!-- “End of the head section.” -->

<body class="d-flex flex-column min-vh-100">
    <!-- “Make the page a full-height vertical flex layout.” -->

    <div class="container mb-5 mt-5">
        <h1 class="text-center text-primary display-4 custom-font">Read View</h1>
        <!-- “Main heading for the Read page.” -->
    </div>

    <nav class="container">
        <div class="row justify-content-center w-100 mb-4">
            <div class="col-3 d-grid">
                <a href="/home/create" class="btn btn-primary">Create View</a>
            </div>
            <div class="col-3 d-grid">
                <a href="/home/read" class="btn btn-primary">Read View</a>
            </div>
            <div class="col-3 d-grid">
                <a href="/home/update" class="btn btn-primary">Update View</a>
            </div>
            <div class="col-3 d-grid">
                <a href="/home/delete" class="btn btn-primary">Delete View</a>
            </div>
        </div>
    </nav>
    <!-- “Navigation buttons for CRUD views.” -->

    <div class="container flex-grow-1 py-5">
        <h2 class="mb-4">All Users </h2>
        <!-- “Section title for the user list.” -->

        @if (Model != null && Model.Any())
        {
            <div class="table-responsive">
                <table class="table table-bordered table-striped align-middle text-center">
                    <thead class="table-dark">
                        <tr>
                            <th>Name</th>
                            <th>Email</th>
                            <th>Phone</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var user in Model)
                        {
                            <tr>
                                @using (Html.BeginForm("UpdateOperation", "Home", FormMethod.Post))
                                {
                                    @Html.AntiForgeryToken()
                                    @Html.Hidden("Id", user.Id)
                                    <td>
                                        <input name="Name" class="form-control" value="@user.Name" />
                                    </td>
                                    <td>
                                        <input name="Email" type="email" class="form-control" value="@user.Email" />
                                    </td>
                                    <td>
                                        <input name="PhoneNumber" class="form-control" value="@user.PhoneNumber" />
                                    </td>
                                }
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
    </div>
    <!-- “Show user data inside a responsive Bootstrap table, with inline form support.” -->

    <footer class="container mt-auto mb-5">
        <small class="d-block text-center text-primary">&copy; 2025 UserManagementCrudApp</small>
        <!-- “Page footer with copyright.” -->
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
    <!-- “Bootstrap JS bundle for interactive elements.” -->

</body>
</html>
<!-- “End of the HTML document.” -->
